interface:
  display_name: "Technical Diagram Generator"
  short_description: "Create source-backed, audience-calibrated diagrams."
  default_prompt: "Use $technical-diagram-generator: author Draw.io sources manually when requested, preserve editable sources, export through the bundled adapter, and run strict layout, parity, and page-width/100% visual checks. Do not auto-layout or generate Draw.io XML from a component spec."
