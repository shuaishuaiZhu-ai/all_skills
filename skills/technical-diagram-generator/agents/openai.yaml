interface:
  display_name: "Technical Diagram Generator"
  short_description: "Create source-backed, audience-calibrated diagrams."
  default_prompt: "Use $technical-diagram-generator: build the .drawio source with scripts/drawiokit.py (it sizes cards and refuses layout violations; rows and their order are yours), export with scripts/export-drawio.cjs --final-only so the figure directory holds only .drawio + .drawio.png, preserve any existing hand-edited source as a .generated candidate, and do the page-width/100% visual check before delivering."
